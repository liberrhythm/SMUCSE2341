/*
#include "tag.h"

backScore = 5;
chestScore = 8;
shoulderScore = 10;
gunScore = 15;

Tag::Tag(int tagger, int tagged, int tagNumber) {
    taggerID = tagger;
    taggedID = tagged;
    tagType = tagNumber;
    tagScore = calculateTag();
}

int Tag::calculateTag() {
    switch (tagType) {
        case 1:
            return backScore;
        case 2:
            return chestScore;
        case 3:
            return shoulderScore;
        case 4:
            return gunScore;
    }
}
*/
